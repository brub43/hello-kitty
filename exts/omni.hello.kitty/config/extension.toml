[package]
# Semantic Versioning is used: https://semver.org/
version = "1.0.0"

# The title and description fields are primarily for displaying extension info in UI
title = "Hello Kitty CLI"
description = "The simplest python extension example that doesn't use UI or do anything but console output"

# Path (relative to the root) or content of readme markdown file for UI.
readme  = "docs/README.md"

# URL of the extension source repository.
repository = "https://github.com/jordanhubbard/hello-kitty"

# One of categories for UI.
category = "Example"

# Keywords for the extension
keywords = ["kit", "example"]

# Icon to show in the extension manager
icon = "data/icon.png"

# Preview to show in the extension manager
preview_image = "data/preview.png"


[dependencies]

# Main python module this extension provides, it will be publicly available as "import omni.hello.kitty".
[[python.module]]
name = "omni.hello.kitty"

[[test]]
# Extra dependencies only to be used during test run
dependencies = [
]
